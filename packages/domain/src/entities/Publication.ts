export type PublicationId = string;

export enum PublicationType {
  MIRROR = 'mirror',
  POST = 'post',
  COMMENT = 'comment',
}

export enum ReactionType {
  UPVOTE = 'upvote',
  DOWNVOTE = 'downvote',
}
